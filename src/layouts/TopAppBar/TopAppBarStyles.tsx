import { AppBar, styled } from "@mui/material";

export const StyledAppBar = styled(AppBar)`
  background-color: rgba(2, 31, 220, 0.33);
  backdrop-filter: blur(10px);
  position: sticky;
  top: 0;
`;